HPC website — TanStack Start version
====================================

Drag the "src" folder in this directory into GitHub. Not this text file,
not the parent folder — just "src".

Before you do, delete the "src 2" folder that is currently in the repo.
It was an accident from the last upload and nothing reads it.

These 19 files either replace a Lovable file at the same path or are new:

  REPLACED
    src/routes/__root.tsx      Lovable's app shell, plus the site header,
                               footer and the HPC stylesheet links.
    src/routes/index.tsx       was the blank page; now the homepage.
    src/routeTree.gen.ts       regenerated so it knows all seven routes.

  NEW
    src/routes/campaigns.tsx   /campaigns
    src/routes/pricing.tsx     /pricing
    src/routes/wins.tsx        /wins
    src/routes/resources.tsx   /resources
    src/routes/about.tsx       /about
    src/routes/scorecard.tsx   /scorecard
    src/components/*           header, footer, calculator, wins, forms
    src/data/*                 member wins + scorecard content
    src/config/links.ts        SET BOOKING_URL HERE before launch
    src/hpc-design-system.css  the approved design system

NOT INCLUDED, deliberately — these stay exactly as Lovable wrote them:
    src/router.tsx, src/styles.css, src/lib/, src/server.ts, src/start.ts,
    src/components/ui/, package.json, vite.config.ts, tsconfig.json

No dependencies are added. Nothing outside src/ changes.
